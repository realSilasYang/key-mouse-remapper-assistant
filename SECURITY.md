# 安全政策

## 支持版本

安全修复只发布到最新稳定版本。

## 报告漏洞

请使用仓库的私密安全报告功能，不要在公开 Issue 中提交利用代码、个人路径、按键内容、
设备接口路径或其他敏感信息。报告应包含受影响版本、复现条件、影响和建议缓解措施。

## 权限与输入边界

键鼠重映射小助手默认以当前普通用户运行，不安装服务、内核驱动或系统钩子。可选的
“以管理员身份重新启动”只改善与已提升应用交互时的权限一致性，不会让 Raw Input 获得
抑制原输入、安全桌面或跨会话输入能力。

映射区域只接受 managed RuleSpec 注释块，不执行用户写入或规则包携带的 AHK 代码。
RuleSpec 的 `run` 动作即使结构合法，也必须通过运行时安全策略。规则块和规则包中的
SHA-256 用于发现损坏，不是发布者签名，也不证明规则可信。

发行包不联网、不收集遥测。事件缓冲默认只存在于当前进程内，只有用户主动导出时才写入
文件。诊断和设备证据仍可能包含时间、应用身份和设备派生标识，分享前应检查内容。

完整能力边界、资源上限和恢复语义见
[`docs/security-and-limits.md`](docs/security-and-limits.md)。
